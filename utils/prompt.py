def promptToAi(language, model_attributes):
    prompt = f"Generate an Express {language} model with his attributes {model_attributes}, \n Please only provide the code without explanation."
    return prompt